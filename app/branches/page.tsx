'use client';
import {useState} from 'react';
import {MapPin,ArrowUpRight,Phone,MessageCircle} from 'lucide-react';
import {Header,Footer} from '../page';
import {branches} from './data';
export default function Branches(){let [selected,setSelected]=useState(0);let b=branches[selected];return <><Header/><main className="wrap"><div className="page-intro"><span className="eyebrow">A LITTLE CLOSER TO YOU</span><h1>成長的好地方，就在你身邊</h1><p>選擇分會，計劃與寶寶的下一次探索。</p></div><div className="columns section"><div className="stack">{branches.map((x,i)=><div className={'panel '+(i===selected?'branch-active':'')} key={x.en}><button className="branch-choice" onClick={()=>setSelected(i)} aria-pressed={i===selected}><span className="eyebrow">0{i+1} / {x.en}</span><h3>{x.name}分會 <MapPin size={18} style={{display:'inline'}}/></h3><p>{x.address}</p></button><p className="muted">{x.hours}</p><div className="row"><a className="text-link" href={'tel:+852'+x.phone}><Phone size={14} style={{display:'inline'}}/> {x.phone}</a><a className="button small" href={'https://wa.me/852'+x.wa}><MessageCircle size={16}/> WhatsApp</a></div></div>)}</div><div><iframe className="map" title={b.name+'分會地圖'} src={'https://maps.google.com/maps?q='+encodeURIComponent(b.address)+'&output=embed'} loading="lazy" referrerPolicy="no-referrer-when-downgrade"/><div className="panel" style={{marginTop:20}}><h3>{b.name}分會</h3><p>{b.address}</p>{b.transport&&<p>{b.transport}</p>}<a className="button outline" href={'https://www.google.com/maps/dir/?api=1&destination='+encodeURIComponent(b.address)} target="_blank" rel="noreferrer">規劃路線 <ArrowUpRight size={18}/></a></div><p className="muted" style={{marginTop:20}}>其他地區亦設有分會。<a className="text-link" href="https://www.cpda.com.hk/contact-us.html">查看全部分會 ↗</a></p></div></div></main><Footer/></>}
